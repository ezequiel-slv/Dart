void main(){
  var coordenadas = [20, 5]; // define os valores no array

  if(coordenadas case [int x, int y] when x >= 0 && y >= 0){

    // if(coordenadas case [int x, int y]: guarda os valores respectivamente em x e y

    // when x >= 0 && y >= 0): garante que só serão valores inteiros

    print("Coordenadas: $x, $y");
  }else{
    print("Coordenadas inválidas!");
  }
}
